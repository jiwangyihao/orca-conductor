# 进度台账 — <任务短名>

> worker 写，coordinator 读。这份文件的唯一目的：让任何一次意外中断的代价从"整轮重做"降到"接着上一行做"。
> 规则：每完成一个可验证单元就追加一行，**先落盘再继续下一步**。只追加，不重写历史行。

- dispatch: <ctx_xxx>
- task: <task_xxx>
- worktree: <绝对路径>
- 基线: <sha>
- brief: <绝对路径>（sha256: <…>）

## 状态

当前阶段：<Phase n / 步骤名>
下一个动作：<具体到可以直接执行的一句话>
阻塞：<无 / 描述 + 已用 ask 提问的时间>

## 日志（只追加）

| 时间 | 阶段 | 动作 | 结果 / 证据路径 |
|---|---|---|---|
| <HH:MM> | Phase 0 | 读完 brief + AGENTS.md，回写 read-back | 等待 reply |
| <HH:MM> | Phase 1 | 执行 `<cmd>` | 退出码 0，日志 `<path>` |
| <HH:MM> | Phase 1 | 改 `<file>` | commit `<sha>` |

## 已产出

| 路径 | 说明 |
|---|---|
| <path> | <…> |

## 未决与风险

- <一条一行，写清"卡在哪"，不要只写"有点问题">

## 恢复提示（给中断后接手的自己或下一个 worker）

- 已完成到：<阶段 / 具体到哪一步>
- 不要重做：<已确认完成的部分>
- 从这里开始：<下一个动作>
- 已知不能用的做法：<…>
